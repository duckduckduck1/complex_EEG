-- Optional dev seed data. Safe to re-run only on empty database.

-- No default users are seeded here. Create admin via:
--   cd server && python -m app.cli users create --username admin --role admin
